# @package _global_

defaults:
  - override /policy: sS_policy
  - override /param_search: nsgaii_sampler
  - override /metric_ranges: oufo_only
  - override /demand: uclh_tx
  - override /stock: uclh

exp_name: uclh_demand_aoa_no_returns_ss

rollout_wrapper:
  env_params:
    slippage: 0.00
    return_probability: 0.00
