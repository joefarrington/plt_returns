# @package _global_
defaults:
  - override /metric_ranges: oufo_only
  - override /demand: uclh_tx
  - override /stock: rs

exp_name: uclh_demand_rs_aoa_no_returns_q

rollout_wrapper:
  env_params:
    slippage: 0.00
    return_probability: 0.00