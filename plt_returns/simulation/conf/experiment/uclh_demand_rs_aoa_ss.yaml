# @package _global_

defaults:
  - override /policy: sS_policy
  - override /param_search: nsgaii_sampler
  - override /demand: uclh_txr
  - override /stock: rs

exp_name: uclh_demand_rs_aoa_ss

rollout_wrapper:
  env_params:
    slippage: 0.07
    return_probability: 0.08
